<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
 "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mapper.FileMapper">
	<select id="AllSearch" resultType="string">
		SELECT fid FROM filemem WHERE fid ='abcd123'
	</select>
	
	<select id="test" parameterType="java.lang.String" resultType="ArrayList">
		SELECT dong FROM zipcode_t WHERE dong = #{dong}
	</select>
	
	<select id="showUser" parameterType="java.lang.String" resultType="java.lang.String">
		SELECT fid FROM filemem
		WHERE NOT fid IN (
		<foreach collection="list" item="item" separator=",">
  		 #{item}
	  	</foreach>
	  	)
	</select>
	
	<select id="loginchck" statementType="CALLABLE" parameterType="java.lang.String">
      {CALL proc_login(#{p_id, mode=IN, jdbcType=VARCHAR, javaType=String}
                  , #{p_pw, mode=IN, jdbcType=VARCHAR, javaType=String}
                  , #{msg, mode=OUT, jdbcType=VARCHAR, javaType=String})}
   </select>
	
	<!-- 프로시져 예제  -->
	<select id="chat_login" statementType="CALLABLE" parameterType="java.util.Map">
      {CALL proc_login(#{p_no, mode=IN, jdbcType=VARCHAR, javaType=String}
                  , #{p_pw, mode=IN, jdbcType=VARCHAR, javaType=String}
                  , #{msg, mode=OUT, jdbcType=VARCHAR, javaType=String})}
   </select>
   <!-- 프로시져 예제  -->
</mapper>